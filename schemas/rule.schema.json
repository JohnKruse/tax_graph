{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "rule.schema.json",
  "title": "Tax Graph Rule",
  "description": "A reusable, declarative transformation referenced by edges. Expressed with a small primitive instruction set rather than bespoke code. NOTE: the operation enum below is v0 and is being validated/expanded by an IRS-instruction survey (see docs).",
  "type": "object",
  "additionalProperties": false,
  "required": ["rule_id", "operation", "description"],
  "properties": {
    "rule_id": { "type": "string", "pattern": "^[a-z0-9_]+$" },
    "operation": {
      "type": "string",
      "description": "v0 primitive set. Bounding is done with MIN/MAX whose operands may be node refs OR literal constants, so CAP/FLOOR/LIMIT were dropped as redundant (an upper bound is MIN(x, const); a lower bound is MAX(x, const); a clamp is MIN(MAX(x, lo), hi)). Rounding is ROUND with parameters {mode: nearest|up|down, increment} — which also covers round-up/down-to-increment, so CEILING and FLOOR-as-rounding were dropped too.",
      "enum": [
        "COPY", "SUM", "SUBTRACT", "MULTIPLY", "DIVIDE",
        "MIN", "MAX", "NEGATE", "ABS", "ROUND",
        "LOOKUP_TABLE", "LOOKUP_BRACKET",
        "IF", "IF_ELSE", "AND", "OR", "NOT",
        "COMPARE", "REQUIRE_INPUT"
      ]
    },
    "description": { "type": "string" },
    "parameters": { "type": "object", "description": "Operation-specific parameters." },
    "rounding": {
      "type": "string",
      "enum": ["none", "currency", "dollar", "cents"],
      "default": "none"
    },
    "citation_refs": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Per governance (req. doc §13), every supported rule must cite an IRS source."
    }
  }
}
